#!/usr/bin/python3
"""
================================
module to know if isinstance
================================
"""


def is_same_class(obj, a_class):
    """Method that return True if an object is an instance of a class"""

    return type(obj) == a_class
